# local_text_sync

Cross-platform application for text synchronization.
It launches an HTTP server with an HTML page that can be accessed via browser over the local network to synchronize the page's text with the text in the application's window.